"""Declared support ranges for Manim Dock (G1 / V3 Phase 11).

Keep in sync with docs/version-matrix.md.
"""

from __future__ import annotations

# Mirrored from docs/version-matrix.md — update both when bumping support.
MANIM_CE_MIN = "0.18"
MANIM_CE_SUPPORTED_LABEL = "≥ 0.18 (Community)"
MANIM_CE_VERIFIED = "0.20.1"

PYTHON_MIN = "3.10"
PYTHON_SUPPORTED_LABEL = "≥ 3.10"

VSCODE_ENGINE_MIN = "1.85"
VSCODE_ENGINE_SUPPORTED_LABEL = "≥ 1.85"

LIBCST_MIN = "1.1"
LIBCST_SUPPORTED_LABEL = "≥ 1.1"
